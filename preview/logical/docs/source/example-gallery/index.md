# Example gallery

One tile per shipped example under `examples/`, each embedding the real,
test-executed source with one outcome and one evidence-boundary paragraph.
The [gallery overview](../examples.md) presents the same tiles as a grid,
grouped by task; the pages in this section are the individual tiles.
