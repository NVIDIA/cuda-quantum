diff --git a/pyproject.toml b/pyproject.toml
index 5af6015a..a4382a18 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=42", "cmake>=3.18", "ninja"]
+requires = ["setuptools>=42", "cmake>=3.18,<3.29", "ninja"]
 build-backend = "setuptools.build_meta"
 
 
