# ============================================================================ #
# Copyright (c) 2022 - 2024 NVIDIA Corporation & Affiliates.                   #
# All rights reserved.                                                         #
#                                                                              #
# This source code and the accompanying materials are made available under     #
# the terms of the Apache License 2.0 which accompanies this distribution.     #
# ============================================================================ #

name: iqm
description: "CUDA-Q target for IQM."
config:
  # Tell DefaultQuantumPlatform what QPU subtype to use
  platform-qpu: remote_rest
  # Tell NVQ++ to generate glue code to set the target backend name
  gen-target-backend: true
  # Add the rest-qpu library to the link list
  link-libs: ["-lcudaq-rest-qpu"]
  # Specify the gate set to use
  gate-set: iqm
  # Specify that we want to do qubit mapping for the file based on the architecture parameter
  qubit-mapping: file(%QPU_ARCH%)
  # Tell the rest-qpu that we are generating IQM JSON.
  codegen-emission: iqm
  # Library mode is only for simulators, physical backends must turn this off
  library-mode: false

target-arguments:
  - key: server-url
    required: false
    type: string
    platform-arg: url 
    help-string: "Specify URL."
  - key: machine
    required: false
    type: string
    platform-arg: qpu-architecture 
    help-string: "Specify the IQM QPU."
